// Funciones para manejar el modal de Talla S
document.addEventListener('DOMContentLoaded', function() {
    // Modal Talla S
    const btnInfoS = document.getElementById('btnInfoS');
    const modalS = document.getElementById('modalS');
    const closeS = document.querySelector('.closeS');
    const btnCerrarModalS = document.getElementById('btnCerrarModalS');

    if (btnInfoS) {
        btnInfoS.addEventListener('click', () => {
            if (modalS) {
                modalS.classList.remove('hidden');
                modalS.classList.add('show');
            }
        });
    }

    if (closeS) {
        closeS.addEventListener('click', () => {
            if (modalS) {
                modalS.classList.add('hidden');
                modalS.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalS) {
        btnCerrarModalS.addEventListener('click', () => {
            if (modalS) {
                modalS.classList.add('hidden');
                modalS.classList.remove('show');
            }
        });
    }

    // Modal Talla M
    const btnInfoM = document.getElementById('btnInfoM');
    const modalM = document.getElementById('modalM');
    const closeM = document.querySelector('.closeM');
    const btnCerrarModalM = document.getElementById('btnCerrarModalM');

    if (btnInfoM) {
        btnInfoM.addEventListener('click', () => {
            if (modalM) {
                modalM.classList.remove('hidden');
                modalM.classList.add('show');
            }
        });
    }

    if (closeM) {
        closeM.addEventListener('click', () => {
            if (modalM) {
                modalM.classList.add('hidden');
                modalM.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalM) {
        btnCerrarModalM.addEventListener('click', () => {
            if (modalM) {
                modalM.classList.add('hidden');
                modalM.classList.remove('show');
            }
        });
    }

    // Modal Talla L
    const btnInfoL = document.getElementById('btnInfoL');
    const modalL = document.getElementById('modalL');
    const closeL = document.querySelector('.closeL');
    const btnCerrarModalL = document.getElementById('btnCerrarModalL');

    if (btnInfoL) {
        btnInfoL.addEventListener('click', () => {
            if (modalL) {
                modalL.classList.remove('hidden');
                modalL.classList.add('show');
            }
        });
    }

    if (closeL) {
        closeL.addEventListener('click', () => {
            if (modalL) {
                modalL.classList.add('hidden');
                modalL.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalL) {
        btnCerrarModalL.addEventListener('click', () => {
            if (modalL) {
                modalL.classList.add('hidden');
                modalL.classList.remove('show');
            }
        });
    }

    // Modal Talla XL
    const btnInfoXL = document.getElementById('btnInfoXL');
    const modalXL = document.getElementById('modalXL');
    const closeXL = document.querySelector('.closeXL');
    const btnCerrarModalXL = document.getElementById('btnCerrarModalXL');

    if (btnInfoXL) {
        btnInfoXL.addEventListener('click', () => {
            if (modalXL) {
                modalXL.classList.remove('hidden');
                modalXL.classList.add('show');
            }
        });
    }

    if (closeXL) {
        closeXL.addEventListener('click', () => {
            if (modalXL) {
                modalXL.classList.add('hidden');
                modalXL.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalXL) {
        btnCerrarModalXL.addEventListener('click', () => {
            if (modalXL) {
                modalXL.classList.add('hidden');
                modalXL.classList.remove('show');
            }
        });
    }

    // Modal Vainilla
    const btnInfoV = document.getElementById('btnInfoV');
    const modalV = document.getElementById('modalV');
    const closeV = document.querySelector('.closeV');
    const btnCerrarModalV = document.getElementById('btnCerrarModalV');

    if (btnInfoV) {
        btnInfoV.addEventListener('click', () => {
            if (modalV) {
                modalV.classList.remove('hidden');
                modalV.classList.add('show');
            }
        });
    }

    if (closeV) {
        closeV.addEventListener('click', () => {
            if (modalV) {
                modalV.classList.add('hidden');
                modalV.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalV) {
        btnCerrarModalV.addEventListener('click', () => {
            if (modalV) {
                modalV.classList.add('hidden');
                modalV.classList.remove('show');
            }
        });
    }

    // Modal Chocolate
    const btnInfoC = document.getElementById('btnInfoC');
    const modalC = document.getElementById('modalC');
    const closeC = document.querySelector('.closeC');
    const btnCerrarModalC = document.getElementById('btnCerrarModalC');

    if (btnInfoC) {
        btnInfoC.addEventListener('click', () => {
            if (modalC) {
                modalC.classList.remove('hidden');
                modalC.classList.add('show');
            }
        });
    }

    if (closeC) {
        closeC.addEventListener('click', () => {
            if (modalC) {
                modalC.classList.add('hidden');
                modalC.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalC) {
        btnCerrarModalC.addEventListener('click', () => {
            if (modalC) {
                modalC.classList.add('hidden');
                modalC.classList.remove('show');
            }
        });
    }

    // Modal Red Velvet
    const btnInfoR = document.getElementById('btnInfoR');
    const modalR = document.getElementById('modalR');
    const closeR = document.querySelector('.closeR');
    const btnCerrarModalR = document.getElementById('btnCerrarModalR');

    if (btnInfoR) {
        btnInfoR.addEventListener('click', () => {
            if (modalR) {
                modalR.classList.remove('hidden');
                modalR.classList.add('show');
            }
        });
    }

    if (closeR) {
        closeR.addEventListener('click', () => {
            if (modalR) {
                modalR.classList.add('hidden');
                modalR.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalR) {
        btnCerrarModalR.addEventListener('click', () => {
            if (modalR) {
                modalR.classList.add('hidden');
                modalR.classList.remove('show');
            }
        });
    }

    // Modal Chocolate Blanco
    const btnInfoB = document.getElementById('btnInfoB');
    const modalB = document.getElementById('modalB');
    const closeB = document.querySelector('.closeB');
    const btnCerrarModalB = document.getElementById('btnCerrarModalB');

    if (btnInfoB) {
        btnInfoB.addEventListener('click', () => {
            if (modalB) {
                modalB.classList.remove('hidden');
                modalB.classList.add('show');
            }
        });
    }

    if (closeB) {
        closeB.addEventListener('click', () => {
            if (modalB) {
                modalB.classList.add('hidden');
                modalB.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalB) {
        btnCerrarModalB.addEventListener('click', () => {
            if (modalB) {
                modalB.classList.add('hidden');
                modalB.classList.remove('show');
            }
        });
    }

    // Modal Chocolate Oscuro
    const btnInfoOS = document.getElementById('btnInfoOS');
    const modalOS = document.getElementById('modalOS');
    const closeOS = document.querySelector('.closeOS');
    const btnCerrarModalOS = document.getElementById('btnCerrarModalOS');

    if (btnInfoOS) {
        btnInfoOS.addEventListener('click', () => {
            if (modalOS) {
                modalOS.classList.remove('hidden');
                modalOS.classList.add('show');
            }
        });
    }

    if (closeOS) {
        closeOS.addEventListener('click', () => {
            if (modalOS) {
                modalOS.classList.add('hidden');
                modalOS.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalOS) {
        btnCerrarModalOS.addEventListener('click', () => {
            if (modalOS) {
                modalOS.classList.add('hidden');
                modalOS.classList.remove('show');
            }
        });
    }

    // Modal Arequipe
    const btnInfoA = document.getElementById('btnInfoA');
    const modalA = document.getElementById('modalA');
    const closeA = document.querySelector('.closeA');
    const btnCerrarModalA = document.getElementById('btnCerrarModalA');

    if (btnInfoA) {
        btnInfoA.addEventListener('click', () => {
            if (modalA) {
                modalA.classList.remove('hidden');
                modalA.classList.add('show');
            }
        });
    }

    if (closeA) {
        closeA.addEventListener('click', () => {
            if (modalA) {
                modalA.classList.add('hidden');
                modalA.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalA) {
        btnCerrarModalA.addEventListener('click', () => {
            if (modalA) {
                modalA.classList.add('hidden');
                modalA.classList.remove('show');
            }
        });
    }

    // Modal Topping Chispas
    const btnInfoT1 = document.getElementById('btnInfoT1');
    const modalT1 = document.getElementById('modalT1');
    const closeT1 = document.querySelector('.closeT1');
    const btnCerrarModalT1 = document.getElementById('btnCerrarModalT1');

    if (btnInfoT1) {
        btnInfoT1.addEventListener('click', () => {
            if (modalT1) {
                modalT1.classList.remove('hidden');
                modalT1.classList.add('show');
            }
        });
    }

    if (closeT1) {
        closeT1.addEventListener('click', () => {
            if (modalT1) {
                modalT1.classList.add('hidden');
                modalT1.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalT1) {
        btnCerrarModalT1.addEventListener('click', () => {
            if (modalT1) {
                modalT1.classList.add('hidden');
                modalT1.classList.remove('show');
            }
        });
    }

    // Modal Topping Oreo
    const btnInfoT2 = document.getElementById('btnInfoT2');
    const modalT2 = document.getElementById('modalT2');
    const closeT2 = document.querySelector('.closeT2');
    const btnCerrarModalT2 = document.getElementById('btnCerrarModalT2');

    if (btnInfoT2) {
        btnInfoT2.addEventListener('click', () => {
            if (modalT2) {
                modalT2.classList.remove('hidden');
                modalT2.classList.add('show');
            }
        });
    }

    if (closeT2) {
        closeT2.addEventListener('click', () => {
            if (modalT2) {
                modalT2.classList.add('hidden');
                modalT2.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalT2) {
        btnCerrarModalT2.addEventListener('click', () => {
            if (modalT2) {
                modalT2.classList.add('hidden');
                modalT2.classList.remove('show');
            }
        });
    }

    // Modal Topping M&M
    const btnInfoT3 = document.getElementById('btnInfoT3');
    const modalT3 = document.getElementById('modalT3');
    const closeT3 = document.querySelector('.closeT3');
    const btnCerrarModalT3 = document.getElementById('btnCerrarModalT3');

    if (btnInfoT3) {
        btnInfoT3.addEventListener('click', () => {
            if (modalT3) {
                modalT3.classList.remove('hidden');
                modalT3.classList.add('show');
            }
        });
    }

    if (closeT3) {
        closeT3.addEventListener('click', () => {
            if (modalT3) {
                modalT3.classList.add('hidden');
                modalT3.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalT3) {
        btnCerrarModalT3.addEventListener('click', () => {
            if (modalT3) {
                modalT3.classList.add('hidden');
                modalT3.classList.remove('show');
            }
        });
    }

    // Modal Topping Chips
    const btnInfoT4 = document.getElementById('btnInfoT4');
    const modalT4 = document.getElementById('modalT4');
    const closeT4 = document.querySelector('.closeT4');
    const btnCerrarModalT4 = document.getElementById('btnCerrarModalT4');

    if (btnInfoT4) {
        btnInfoT4.addEventListener('click', () => {
            if (modalT4) {
                modalT4.classList.remove('hidden');
                modalT4.classList.add('show');
            }
        });
    }

    if (closeT4) {
        closeT4.addEventListener('click', () => {
            if (modalT4) {
                modalT4.classList.add('hidden');
                modalT4.classList.remove('show');
            }
        });
    }

    if (btnCerrarModalT4) {
        btnCerrarModalT4.addEventListener('click', () => {
            if (modalT4) {
                modalT4.classList.add('hidden');
                modalT4.classList.remove('show');
            }
        });
    }

    // Cerrar modal al hacer clic fuera del contenido
    window.addEventListener('click', (event) => {
        // Verificar todos los modales
        document.querySelectorAll('.modal').forEach(modal => {
            if (event.target === modal) {
                modal.classList.add('hidden');
                modal.classList.remove('show');
            }
        });
    });

    // Cerrar modales con la tecla ESC
    document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') {
            document.querySelectorAll('.modal').forEach(modal => {
                modal.classList.add('hidden');
                modal.classList.remove('show');
            });
        }
    });
});
